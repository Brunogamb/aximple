<footer>
    <div class="footer-line"></div>
    <div class="container">
        <div class="brand-info">
            <p class="copyright">
                <strong>AXIMPLE</strong> &copy; {new Date().getFullYear()}
            </p>
        </div>
        
        <div class="dev-info">
            <p>Creado por <a href="https://github.com/BrunoGamb" target="_blank" rel="noopener noreferrer">BrunoGamb</a></p>
        </div>
    </div>
</footer>

<style>
    footer {
        margin-top: 5rem;
        padding-bottom: 3rem;
    }

    .footer-line {
        height: 1px;
        background: linear-gradient(
            to right, 
            transparent, 
            var(--border-soft), 
            transparent
        );
        max-width: 1200px;
        margin: 0 auto 3rem;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    p {
        font-size: 0.9rem;
        color: var(--text-muted); 
    }

    .copyright strong {
        color: var(--text-main);
        letter-spacing: 1px;
        margin-right: 8px;
    }

    .copyright span {
        margin-left: 8px;
        opacity: 0.5;
        font-weight: 300;
    }

    .dev-info p {
        font-size: 0.85rem;
    }

    .dev-info a {
        color: var(--accent-secondary);
        text-decoration: none;
        font-weight: 600;
        transition: all 0.2s ease;
        padding-bottom: 2px;
        border-bottom: 1px solid transparent;
    }

    .dev-info a:hover {
        color: var(--text-main);
        border-bottom-color: var(--accent-secondary);
    }

    @media (max-width: 768px) {
        .container {
            flex-direction: column;
            gap: 1.5rem;
            text-align: center;
        }

        .copyright span {
            display: block;
            margin-top: 0.5rem;
        }
    }
</style>